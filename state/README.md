# Runtime State

`journal_monitor.py` writes `journal_monitor_state.json` here at runtime.

Do not commit real state files. They are machine-local and only record which DOI values have already been reported.
